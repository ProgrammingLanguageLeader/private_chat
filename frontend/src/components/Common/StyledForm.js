import styled from 'styled-components';

const StyledForm = styled.form.attrs({
  className: 'center br1 shadow-5 measure pa5 mt3 primary-bg',
})`
  background-color: rgba(13, 20, 30, 0.8);
`;

export default StyledForm;